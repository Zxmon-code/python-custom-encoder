import time

#The characters that can be encoded
abc = ["a", "b", "c", "d"]

#the list of characters how they get encoded
cba = ["d", "c", "b", "a"]

word = input(str("Which word would you like to encode: "))

word_length = len(word)

list_word = list.word

for i in word